---
import { Image } from 'astro:assets';
import hs2aImg from '../assets/images/HAS-Spots-Hero-2a.webp';
import hs3Img from '../assets/images/HAS-Spots-Hero-3.webp';
import hs2bImg from '../assets/images/HAS-Spots-Hero-2b.webp';
---

<!-- Video Lightbox -->
<div id="hs-lightbox" class="hs-lightbox" aria-hidden="true">
	<div class="hs-lightbox-backdrop" id="hs-backdrop"></div>

	<div class="hs-modal">
		<button class="hs-close" id="hs-close" aria-label="Close">
			<svg xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
				<path d="M18 6 6 18"/><path d="m6 6 12 12"/>
			</svg>
		</button>

		<div class="hs-modal-inner">
			<!-- Video -->
			<div class="hs-video-wrap">
				<div class="hs-video-border"></div>
				<iframe
					id="hs-iframe"
					src=""
					data-src="https://www.youtube-nocookie.com/embed/NDSOrvPXnsQ?autoplay=1"
					title="Haveaspot For Spots Video"
					frameborder="0"
					allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
					allowfullscreen
				></iframe>
			</div>

			<!-- Side content -->
			<div class="hs-modal-content">
				<h2 class="hs-modal-heading">
					List A Spot,<br />Support Your Community.
				</h2>
				<p class="hs-modal-body">
					By listing your space on Haveaspot you open your doors to your local community, generate revenue, and keep your venue thriving.
				</p>
				<a href="https://haveaspot.com/users/sign_up" class="hs-modal-btn">List Your Spot</a>
			</div>
		</div>
	</div>
</div>

<!-- Hero -->
<section class="hs-hero">
	<div class="hs-container">
		<div class="hs-grid">

			<!-- 1: Text -->
			<div class="hs-text hs-item-1">
				<h1 class="hs-heading">
					List Your Space. Earn Revenue. Support Your Community.
				</h1>
				<div class="hs-subtext">
					<p class="hs-sub-headline">Put your spot at the heart of your community.</p>
					<p class="hs-description">
						Haveaspot connects your venue with local bookers looking for great spaces. List for free, set your own rates, and start generating income while giving back to your community.
					</p>
				</div>
				<div class="hs-actions">
					<a href="https://haveaspot.com/users/sign_up" class="hs-btn">List Your Spot</a>
				</div>
			</div>

			<!-- 2a: Top right image -->
			<div class="hs-media hs-item-2a">
				<Image src={hs2aImg} alt="Netball match at a community sports hall" class="hs-panel-img" loading="eager" />
			</div>

			<!-- 3: Video panel -->
			<div class="hs-media hs-item-3">
				<Image src={hs3Img} alt="The Ashby Community Hall surrounded by gardens" class="hs-panel-img" loading="eager" />

				<div class="hs-video-overlay">
					<div class="hs-find-out">
						<h3 class="hs-find-heading">Find Out More</h3>
						<p class="hs-find-body">
							See how Haveaspot helps venue owners like you grow bookings and support their local area.
						</p>
					</div>

					<button class="hs-watch-btn" id="hs-open" aria-label="Watch video">
						<span class="hs-watch-label">Watch A Video</span>
					</button>
				</div>
			</div>

			<!-- 2b: Bottom right image -->
			<div class="hs-media hs-item-2b">
				<Image src={hs2bImg} alt="Children's birthday party at a community hall" class="hs-panel-img" loading="eager" />
			</div>

		</div>
	</div>
</section>

<style>
	/* ─── Section ─────────────────────────────────── */
	.hs-hero {
		width: 100%;
		padding: 2rem 0 4rem;
		background-color: #ffffff;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	@media (min-width: 1024px) { .hs-hero { padding: 2rem 0 6rem; } }

	/* ─── Container ──────────────────────────────── */
	.hs-container {
		max-width: 1536px;
		margin: 0 auto;
		padding: 0 1rem;
		box-sizing: border-box;
	}

	/* ─── Grid ───────────────────────────────────── */
	/*
	  Mobile/tablet: 2 cols, items reordered
	    order 1 — text   (col-span 2)
	    order 2 — video  (col-span 2)
	    order 3 — 2a     (col 1)
	    order 4 — 2b     (col 2)

	  Desktop: 2 cols [3fr 1fr]
	    row 1: text  | 2a
	    row 2: video | 2b
	*/
	.hs-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}

	@media (min-width: 768px) { .hs-grid { gap: 1.5rem; } }
	@media (min-width: 1024px) {
		.hs-grid {
			grid-template-columns: 3fr 1fr;
			gap: 2rem;
		}
	}

	/* ─── Grid item placement ────────────────────── */
	.hs-item-1 {
		grid-column: 1 / -1;
		order: 1;
	}

	@media (min-width: 1024px) {
		.hs-item-1 { grid-column: 1; grid-row: 1; order: unset; }
	}

	.hs-item-2a {
		grid-column: 1;
		order: 3;
	}

	@media (min-width: 1024px) {
		.hs-item-2a { grid-column: 2; grid-row: 1; order: unset; align-self: stretch; }
	}

	.hs-item-3 {
		grid-column: 1 / -1;
		order: 2;
	}

	@media (min-width: 1024px) {
		.hs-item-3 { grid-column: 1; grid-row: 2; order: unset; }
	}

	.hs-item-2b {
		grid-column: 2;
		order: 4;
	}

	@media (min-width: 1024px) {
		.hs-item-2b { grid-column: 2; grid-row: 2; order: unset; }
	}

	/* ─── Text block ─────────────────────────────── */
	.hs-text {
		display: flex;
		flex-direction: column;
		gap: 2rem;
		padding-bottom: 1rem;
	}

	@media (min-width: 1024px) { .hs-text { padding-right: 3rem; padding-bottom: 0; } }

	.hs-heading {
		color: #021300;
		font-size: 2.5rem;
		font-weight: 800;
		line-height: 1.1;
		letter-spacing: -0.02em;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	@media (min-width: 768px)  { .hs-heading { font-size: 3.75rem; } }
	@media (min-width: 1024px) { .hs-heading { font-size: clamp(3rem, 5vw, 4.75rem); } }

	.hs-subtext {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.hs-sub-headline {
		color: #021300;
		font-size: 1.125rem;
		font-weight: 700;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	.hs-description {
		color: #021300;
		font-size: 1rem;
		font-weight: 300;
		line-height: 1.625;
		margin: 0;
		max-width: 48rem;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	/* ─── Button ─────────────────────────────────── */
	.hs-actions { padding-top: 1rem; }

	.hs-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		background-color: #021300;
		color: #ffffff;
		text-decoration: none;
		border: none;
		border-radius: 6px;
		height: 44px;
		width: 100%;
		font-size: 16px;
		font-weight: 500;
		font-family: 'Poppins', system-ui, sans-serif;
		transition: background-color 0.2s ease;
		box-sizing: border-box;
	}

	@media (min-width: 640px) { .hs-btn { width: 150px; } }

	.hs-btn:hover {
		background-color: #0AAD0A;
		text-decoration: none;
		color: #ffffff;
	}

	/* ─── Media panels ───────────────────────────── */
	.hs-media {
		border: 2px solid #021300;
		border-radius: 12px;
		overflow: hidden;
		position: relative;
	}

	/* Top right image — stretches to fill row 1 on desktop */
	.hs-item-2a { height: 250px; }
	@media (min-width: 768px)  { .hs-item-2a { height: 300px; } }
	@media (min-width: 1024px) { .hs-item-2a { height: 100%; } }

	/* Video panel heights */
	.hs-item-3 { height: 320px; }
	@media (min-width: 768px)  { .hs-item-3 { height: 380px; } }
	@media (min-width: 1024px) { .hs-item-3 { height: 380px; } }

	/* Bottom right image — matches video panel height */
	.hs-item-2b { height: 250px; }
	@media (min-width: 768px)  { .hs-item-2b { height: 300px; } }
	@media (min-width: 1024px) { .hs-item-2b { height: 380px; } }

	.hs-placeholder {
		position: absolute;
		inset: 0;
	}

	/* Full-cover image for media panels */
	.hs-panel-img {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 0;
		display: block;
	}

	/* ─── Video panel overlay ────────────────────── */
	.hs-video-overlay {
		position: absolute;
		inset: 0;
		padding: 1.5rem;
		display: flex;
		flex-direction: column;
		justify-content: flex-end;
		gap: 1.5rem;
		z-index: 10;
	}

	@media (min-width: 640px) {
		.hs-video-overlay {
			flex-direction: row;
			align-items: flex-end;
			justify-content: space-between;
			padding: 2rem;
		}
	}

	.hs-find-out {
		background-color: rgba(2, 19, 0, 0.75);
		border-radius: 6px;
		padding: 0.75rem 1rem;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		max-width: 28rem;
	}

	@media (min-width: 640px) { .hs-find-out { padding: 1.5rem; background-color: #021300; } }

	.hs-find-heading {
		color: #ffffff;
		font-size: 1rem;
		font-weight: 700;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	@media (min-width: 640px) { .hs-find-heading { font-size: 1.5rem; } }

	.hs-find-body {
		color: #ffffff;
		font-size: 0.8125rem;
		font-weight: 400;
		line-height: 1.4;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	@media (min-width: 640px) { .hs-find-body { font-size: 1rem; line-height: 1.5; } }

	/* ─── Watch Video button ─────────────────────── */
	.hs-watch-btn {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		background-color: #021300;
		border: none;
		border-radius: 6px;
		height: 44px;
		padding: 0 1.25rem;
		color: #ffffff;
		font-family: 'Poppins', system-ui, sans-serif;
		cursor: pointer;
		transition: background-color 0.2s ease;
		width: 100%;
		flex-shrink: 0;
		box-sizing: border-box;
	}

	@media (min-width: 640px) { .hs-watch-btn { width: 150px; } }

	.hs-watch-btn:hover { background-color: #0AAD0A; }

	.hs-watch-label {
		font-size: 16px;
		font-weight: 500;
		white-space: nowrap;
	}

	/* ─── Lightbox ───────────────────────────────── */
	.hs-lightbox {
		display: none;
		position: fixed;
		inset: 0;
		z-index: 200;
		align-items: center;
		justify-content: center;
		padding: 1rem;
	}

	.hs-lightbox.is-open { display: flex; }

	.hs-lightbox-backdrop {
		position: absolute;
		inset: 0;
		background-color: rgba(0, 0, 0, 0.9);
		backdrop-filter: blur(4px);
	}

	.hs-modal {
		position: relative;
		z-index: 10;
		background-color: #ffffff;
		border: 2px solid #021300;
		border-radius: 12px;
		padding: 4.5rem 2rem 2rem;
		width: 100%;
		max-width: 960px;
		max-height: 90vh;
		overflow-y: auto;
		box-shadow: 0 25px 60px rgba(0, 0, 0, 0.4);
	}

	@media (min-width: 1024px) {
		.hs-modal { padding: 4.5rem 3rem 3rem; overflow-y: visible; }
	}

	.hs-close {
		position: absolute;
		top: 1.25rem;
		right: 1.25rem;
		background: none;
		border: none;
		cursor: pointer;
		color: #021300;
		padding: 0.25rem;
		line-height: 0;
		transition: color 0.2s ease;
	}

	.hs-close:hover { color: #0AAD0A; }
	.hs-close svg { color: inherit; }

	.hs-modal-inner {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	@media (min-width: 1024px) {
		.hs-modal-inner { flex-direction: row; align-items: flex-end; }
	}

	/* ─── Video iframe ───────────────────────────── */
	.hs-video-wrap {
		position: relative;
		width: 100%;
		aspect-ratio: 16 / 9;
		background-color: #000;
		border-radius: 12px;
		overflow: hidden;
		flex-shrink: 0;
	}

	@media (min-width: 1024px) { .hs-video-wrap { width: 65%; } }

	.hs-video-border {
		position: absolute;
		inset: 0;
		border: 2px solid #021300;
		border-radius: 12px;
		pointer-events: none;
		z-index: 10;
	}

	.hs-video-wrap iframe {
		width: 100%;
		height: 100%;
		border: none;
	}

	/* ─── Modal side content ─────────────────────── */
	.hs-modal-content {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	@media (min-width: 1024px) { .hs-modal-content { width: 35%; } }

	.hs-modal-heading {
		color: #021300;
		font-size: 1.75rem;
		font-weight: 700;
		line-height: 1.2;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	@media (min-width: 1024px) { .hs-modal-heading { font-size: 2.25rem; } }

	.hs-modal-body {
		color: #021300;
		font-size: 1rem;
		font-weight: 300;
		line-height: 1.625;
		margin: 0;
		font-family: 'Poppins', system-ui, sans-serif;
	}

	.hs-modal-btn {
		display: flex;
		align-items: center;
		justify-content: center;
		background-color: #021300;
		color: #ffffff;
		text-decoration: none;
		border: none;
		border-radius: 6px;
		height: 44px;
		width: 100%;
		font-size: 16px;
		font-weight: 500;
		font-family: 'Poppins', system-ui, sans-serif;
		transition: background-color 0.2s ease;
		box-sizing: border-box;
	}

	@media (min-width: 640px) { .hs-modal-btn { width: 150px; } }

	.hs-modal-btn:hover {
		background-color: #0AAD0A;
		text-decoration: none;
		color: #ffffff;
	}
</style>

<script>
	(function () {
		const lightbox = document.getElementById('hs-lightbox');
		const backdrop = document.getElementById('hs-backdrop');
		const openBtn  = document.getElementById('hs-open');
		const closeBtn = document.getElementById('hs-close');
		const iframe   = document.getElementById('hs-iframe') as HTMLIFrameElement | null;

		function openLightbox() {
			if (!lightbox || !iframe) return;
			iframe.src = iframe.dataset.src ?? '';
			lightbox.classList.add('is-open');
			document.body.style.overflow = 'hidden';
		}

		function closeLightbox() {
			if (!lightbox || !iframe) return;
			lightbox.classList.remove('is-open');
			iframe.src = '';
			document.body.style.overflow = '';
		}

		openBtn?.addEventListener('click', openLightbox);
		closeBtn?.addEventListener('click', closeLightbox);
		backdrop?.addEventListener('click', closeLightbox);

		document.addEventListener('keydown', (e) => {
			if (e.key === 'Escape') closeLightbox();
		});
	})();
</script>
